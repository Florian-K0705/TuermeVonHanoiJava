1. BinaryTree ist die relevante Datenstruktur. TreeNode wird darin verwendet. Benenne die Klassen so, dass man dies sofort erkennt.

2. Dasselbe wie für 1. gilt auch für GList und GNode. Benenne diese bitte passend um.

3. Implementiere einen Stack (generisch).

4. Implementiere eine Queue [Warteschlange] (generisch).

5. Implementiere einen AVL-Baum ( double ).

6. Prüfe, ob die Vorteile eines B-Baums auch für Java nützlich gemacht werden können. Wenn ja, implementiere einen B-Baum ( double ).

7. Implementiere einen Heap ( double ).

8. Implementiere einen Hash-Tabelle mit geeigneter Hash-Funktion.